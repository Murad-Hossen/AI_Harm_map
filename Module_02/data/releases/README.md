# Frozen derived releases

Add the final derived event corpus only after inference, verification, and
redistribution review are complete.

Recommended files:

```text
ai_harm_events_v1.jsonl
event_embeddings_v1.npz
```

Generate a manifest with counts and SHA-256 hashes.
