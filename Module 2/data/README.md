# Data

This repository does not bundle the complete raw source-article corpus.

- `taxonomy/` contains the authoritative taxonomy workbook.
- `samples/` contains synthetic records for code/tests only.
- `releases/` is reserved for frozen derived event releases after review.
- `manifests/` may contain hashes and counts for frozen releases.

Synthetic samples are not paper observations.
